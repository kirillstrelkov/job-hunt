set dotenv-path := "private/.env"
set dotenv-required := true

cli:
    uv run python cli.py --log-level DEBUG data/private/urls.yml

scli:
    uv run python cli_single.py

test:
    uv run pytest tests/llm_test.py

lint:
    uv run ruff check --fix
    uv run ruff check
