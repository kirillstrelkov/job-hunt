# Run a single model against the prompt file
run model="":
	uv run python {{ justfile_directory() }}/run_single_model.py {{ if model == "" { "" } else { "--model " + model } }}




# Run stats comparison for all configured models
run-all:
	uv run python {{ justfile_directory() }}/run_all_models.py

