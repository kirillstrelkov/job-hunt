# Ollama local models

> NOTE: when pydantic_ai agent is used with local gemma4 models it fails to provide proper response due to context size is too small - passing context or num_ctx to agent as parameters do NOT work so local models should be updated with proper context
